# Parsing (LlamaParse)
LLAMA_CLOUD_API_KEY=

# Embeddings (NVIDIA)
NVIDIA_API_KEY=
EMBEDDING_MODEL=nvidia/llama-nemotron-embed-1b-v2
VECTOR_ENABLED=true

# LLM - Groq (primary) + OpenRouter (fallback)
GROQ_API_KEY=
OPENROUTER_API_KEY=
# Comma-separated provider:model pairs. Tried in order; 429 triggers next.
LLM_FALLBACK_CHAIN=groq:llama-3.3-70b-versatile,groq:llama-3.1-8b-instant,openrouter:meta-llama/llama-3.3-70b-instruct:free,openrouter:openrouter/free

# Advanced RAG (all ON by default, toggle in Admin)
HYDE_ENABLED=true
DEEP_REWRITE_ENABLED=true
DECOMPOSITION_ENABLED=true
RERANKER_ENABLED=true

# Ingestion tuning
INGESTION_PARSE_WORKERS=4
INGESTION_PARSE_QUEUE_SIZE=32
EMBEDDING_BATCH_SIZE=32
VECTOR_UPSERT_BATCH_SIZE=64
BM25_COMMIT_BATCH_SIZE=256
INDEX_SWAP_MODE=atomic_swap

# General
IGNORE_TEST_DEMO_INDEXES=true
CHUNKING_MODE=window
STREAMLIT_PORT=8501
